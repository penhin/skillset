# Domain docs

This is a single-context repository. Before planning or implementing, read the root `CONTEXT.md` and relevant records in `docs/adr/`.

Use the glossary's canonical terms in specifications, tickets, tests, and code. Surface any conflict with an existing ADR rather than silently overriding it.
